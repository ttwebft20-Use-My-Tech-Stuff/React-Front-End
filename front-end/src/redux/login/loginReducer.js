const initialState = {
    username: "",
    password: "",
    remember: false,
}

const loginReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default loginReducer;